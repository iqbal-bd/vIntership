@model IEnumerable<UserRegistration_Login.Models.User>
@{
}
<br />
<div class ="container">
    <div class="row">
        <div class="col-8 offset-2">
            <h1 class="text-center text-success ">User Record</h1>
            <a class="btn btn-sm btn-secondary text-white m-2" asp-controller="User" asp-action="Create">Add New User</a>
            <table class="table table-bordered">
                <tr class="text-center">
                    <th>UserID</th>
                    <th>FirstName</th>
                    <th>LastName</th>
                    <th>FullName</th>
                    <th>Email</th>
                    <th>District</th>
                    <th>PostCode</th>
                    <th>Division</th>
                </tr>
                @foreach(var item in Model)
                {
                    <tr class="text-center">
                        <td>@item.ID</td>
                        <td>@item.FirstName</td>
                        <td>@item.LastName</td>
                        <td>@item.FullName</td>
                        <td>@item.Email</td>
                        <td>@item.District</td>
                        <td>@item.PostCode</td>
                        <td>@item.Division</td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>